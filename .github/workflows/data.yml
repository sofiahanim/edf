name: Data Pipeline

on:
  push:
    branches:
      - main  # Trigger the workflow on pushes to the main branch
  workflow_dispatch:  # Allow manual triggering
  schedule:
    - cron: '0 1 * * *'  # Optional: Run daily at 1 AM UTC

jobs:
  data_pipeline:
    runs-on: ubuntu-20.04

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.11.10

      - name: Install dependencies
        run: |
          python3 -m pip install --upgrade pip
          python3 -m pip install -r requirements_linux.txt

      - name: Verify file existence and content
        run: |
          ls -l data/demand/2025.csv
          cat data/demand/2025.csv || echo "CSV file is empty or missing."

      - name: Commit and push updated CSV
        run: |
          git config --local user.name "GitHub Actions"
          git config --local user.email "actions@github.com"
          git add --force data/demand/2025.csv
          git commit -m "Update CSV with new data" || echo "Nothing to commit."
          git push || echo "No changes pushed."

      - name: Notify success
        if: success()
        run: echo "🎉 Data pipeline completed successfully!"

      - name: Notify failure
        if: failure()
        run: echo "❌ Data pipeline failed. Check logs for details."

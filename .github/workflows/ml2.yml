name: ML Part 2

on:
  push:
    branches:
      - main  # Trigger workflow on pushes to main branch
  schedule:
    - cron: "0 19 * * *"  # Schedule daily at 19:00 UTC (3:00 AM Malaysia Time)
  workflow_dispatch:  # Allow manual triggering

permissions:
  contents: write  # Grants permission to commit and push changes

jobs:
  ml-training:
    runs-on: ubuntu-20.04

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v3

    - name: Set Up Python
      uses: actions/setup-python@v4
      with:
        python-version: "3.11.10"

    - name: Upgrade System Dependencies
      run: |
        echo "🔄 Upgrading system dependencies..."
        sudo apt-get update -y
        sudo apt-get install -y gcc g++ make libopenblas-dev liblapack-dev libblas-dev gfortran
        pip install --only-binary=:all: numpy
      env:
        DEBIAN_FRONTEND: noninteractive

    - name: Install Python Dependencies
      run: |
        echo "🔄 Installing Python dependencies with verbose logging..."
        python3 -m pip install --upgrade pip setuptools wheel
        python3 -m pip install -r requirements_linux.txt --verbose

    - name: Run Model Training Script
      run: |
        echo "🏃 Running the ML training script with verbose output..."
        python3 scripts/ml_part_2.py --verbose
      env:
        PYTHONUNBUFFERED: 1

    - name: Verify Metrics Files
      run: |
        echo "🔍 Verifying metrics files..."
        for file in training/h2o_metrics.csv training/darts_theta_metrics.csv training/prophet_metrics.csv training/training_info.csv; do
          if [ -f "$file" ]; then
            echo "✅ $file exists and is ready."
          else
            echo "❌ $file is missing."
            exit 1
          fi
        done

    - name: Verify Evaluation Files
      run: |
        echo "🔍 Verifying evaluation files..."
        for file in evaluation/h2o_predictions.csv evaluation/darts_theta_predictions.csv evaluation/prophet_predictions.csv evaluation/summary_report.csv; do
          if [ -f "$file" ]; then
            echo "✅ $file exists and is ready."
          else
            echo "❌ $file is missing."
            exit 1
          fi
        done

    - name: Commit and Push Metrics Files
      run: |
        echo "📂 Committing and pushing metrics and evaluation files..."
        git config --global user.name "github-actions[bot]"
        git config --global user.email "github-actions[bot]@users.noreply.github.com"
        git add training/h2o_metrics.csv training/darts_theta_metrics.csv training/prophet_metrics.csv training/training_info.csv || echo "No changes to commit."
        git add evaluation/h2o_predictions.csv evaluation/darts_theta_predictions.csv evaluation/prophet_predictions.csv evaluation/summary_report.csv || echo "No changes to commit."
        git diff --cached --exit-code || git commit -m "Update metrics and evaluation files after training"
        git push || echo "No changes to push."
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    - name: Notify Success
      if: success()
      run: echo "🎉 Model training and evaluation update completed successfully!"

    - name: Notify Failure
      if: failure()
      run: echo "❌ Model training or evaluation update failed. Check logs for details."
